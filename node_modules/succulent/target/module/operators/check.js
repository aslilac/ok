import { Schema } from "../schema";
export function check(x, schema) {
    if (!Schema.check(schema, x)) {
        throw new Error("check assertion failed");
    }
    return x;
}
//# sourceMappingURL=check.js.map