import { Schema } from "../schema";
export function oneOf(x) {
    return new Schema((t) => {
        for (const value of x) {
            if (Object.is(t, value)) {
                return true;
            }
        }
        return false;
    });
}
//# sourceMappingURL=iterables.js.map