import { Schema, SchemaBase } from "../schema";
export declare const $anyobject: Schema<object>;
export declare function $object<T extends object>(template: {
    [K in keyof T]: SchemaBase<T[K]>;
}): Schema<T>;
export declare function $exact<T extends object>(template: {
    [K in keyof T]: SchemaBase<T[K]>;
}): Schema<T>;
